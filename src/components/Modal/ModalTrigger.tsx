import { DialogTrigger } from '@radix-ui/react-dialog'

// export const ModalTrigger = ({ children, ...props }: DialogTriggerProps) => {
//   return <DialogTrigger {...props}>{children}</DialogTrigger>
// }

export const ModalTrigger = DialogTrigger
